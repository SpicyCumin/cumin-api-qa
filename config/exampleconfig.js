const PORT = 'YOUR PORT HERE';

const HOST1 = 'LINK TO DB HOST';

const USER = 'YOUR DB USER';

const PASSWORD = 'YOUR PASSWORD';

const DBPORT = '5432';

const DB = 'DATABASE NAME';

module.exports = { PORT, HOST1, USER, PASSWORD, DBPORT, DB }